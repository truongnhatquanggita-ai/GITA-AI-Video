const form = document.querySelector('#form'); const out = document.querySelector('#out');
form.addEventListener('submit', async (event) => { event.preventDefault(); out.textContent = 'Đang gửi...'; const response = await fetch('/api/v1/productions', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Owner-Api-Key': localStorage.getItem('gita_api_key') || prompt('Owner API key') || '' }, body: JSON.stringify({ projectId: document.querySelector('#projectId').value, title: document.querySelector('#title').value, prompt: document.querySelector('#prompt').value }) }); out.textContent = JSON.stringify(await response.json(), null, 2); });
